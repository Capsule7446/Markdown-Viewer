---
// Image / diagram lightbox overlay. Behaviour lives in scripts/lightbox.ts.
---
<div id="lightbox" class="lightbox" hidden>
  <div class="lightbox-inner" id="lightbox-inner"></div>
  <!-- Toolbar buttons are generated by scripts/lightbox.ts (shared with the
       in-figure bars) so the glyphs/labels live in one place. -->
  <div class="mzoom-bar lightbox-bar" id="lightbox-bar"></div>
</div>
